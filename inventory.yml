---
all:
  hosts:
    zotac:
      ansible_host: 10.0.1.20
    proliant:
      ansible_host: ubuntu-server.home.arpa
  vars:
    ansible_user: joe
  children:
    docker:
      hosts:
        zotac:
        proliant:
    frigate:
      hosts:
        proliant:
      vars:
        docker_image: ghcr.io/blakeblackshear/frigate:0.12.0-beta8-tensorrt
    mosquitto:
      hosts:
        zotac:
    homeassistant:
      hosts:
        zotac:
    homebridge:
      hosts:
        zotac:
    unifi:
      hosts:
        proliant:
    prometheus:
      hosts:
        proliant:
