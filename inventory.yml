---
all:
  hosts:
    zotac:
      ansible_host: 10.0.1.20
    proliant:
      ansible_host: 10.0.1.10
  vars:
    ansible_user: joe
  children:
    docker:
      hosts:
        zotac:
        proliant:
    frigate:
      hosts:
        proliant:
      vars:
        docker_image: blakeblackshear/frigate:stable
    mosquitto:
      hosts:
        zotac:
    homeassistant:
      hosts:
        zotac:
    homebridge:
      hosts:
        zotac:
